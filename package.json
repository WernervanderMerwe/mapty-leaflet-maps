{
  "name": "mapty-leaflet-maps",
  "description": "Mapty application using leaflet maps API to track exercise",
  "source": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build --dist-dir ./output"
  },
  "devDependencies": {
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "leaflet": "^1.8.0"
  }
}
